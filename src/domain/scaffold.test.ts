// Placeholder so `npm test` passes in Phase 0; real domain tests arrive in Phase 1.
import { describe, expect, it } from 'vitest'

describe('scaffold', () => {
  it('runs tests', () => {
    expect(true).toBe(true)
  })
})
